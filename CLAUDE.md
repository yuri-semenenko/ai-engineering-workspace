# CLAUDE.md

The Claude Code adapter for this repository. `AGENTS.md` is the contract and owns
every repository-wide rule; Claude Code reads `CLAUDE.md` rather than `AGENTS.md`,
so this file imports it. Anything Claude-only belongs here; anything that applies
to any assistant belongs in `AGENTS.md`. See `AGENTS.md` § Instruction layers for
the other two files named `CLAUDE.md` in this tree.

@AGENTS.md
